name: Build and push Docker image
on:
  push:
    branches: [ master ]

  workflow_dispatch:

env:
  RCLONE_CONFIG_MYGDRIVE_CLIENT_ID: ${{ secrets.RCLONE_CLIENT_ID }}
  RCLONE_CONFIG_MYGDRIVE_CLIENT_SECRET: ${{ secrets.RCLONE_CLIENT_SECRET }}
  RCLONE_CONFIG_MYGDRIVE_TOKEN: ${{ secrets.RCLONE_TOKEN }}
  RCLONE_CONFIG_MYGDRIVE_SCOPE: drive

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          push: true
          tags: ghcr.io/ibrutedude/colab-workspace:latest
